#include "main.h"
/**
 * main - test a function that prints if an integer 0 is positive
 * or negative.
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
