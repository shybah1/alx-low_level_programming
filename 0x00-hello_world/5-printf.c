#include <stdio.h>
/**
 * main - Entry point 
 * use function printf
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
